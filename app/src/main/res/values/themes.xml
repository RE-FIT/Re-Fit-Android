<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ReFit" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="bottomNavigationStyle">@style/CustomBottomNavigation</item>
        <!-- Status Bar Default -->
        <item name="android:statusBarColor">@color/default_dark</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:backgroundDimAmount">0.4</item>
    </style>

    <!-- Splash Screen -->
    <style name="Theme.ReFit.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/green1</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_app_240</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.ReFit</item>
    </style>

    <style name="Theme.ReFit" parent="Base.Theme.ReFit" />
</resources>